<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="hello.corebanking.domain.customer.repository.MemberMapper">

    <resultMap id="memberResultMap" type="hello.corebanking.domain.customer.entity.Member">
        <id     property="memberId"   column="member_id"/>
        <result property="loginId"    column="login_id"/>
        <result property="password"   column="password"/>
        <result property="name"       column="name"/>
        <result property="createdAt"  column="created_at"/>
        <result property="updatedAt"  column="updated_at"/>
    </resultMap>

    <insert id="insert" parameterType="hello.corebanking.domain.customer.entity.Member"
            useGeneratedKeys="true" keyProperty="memberId">
        INSERT INTO member (login_id, password, name)
        VALUES (#{loginId}, #{password}, #{name})
    </insert>

    <select id="findByLoginId" parameterType="String" resultMap="memberResultMap">
        SELECT member_id, login_id, password, name, created_at, updated_at
        FROM member
        WHERE login_id = #{loginId}
    </select>

    <select id="existsByLoginId" parameterType="String" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM member
        WHERE login_id = #{loginId}
    </select>

</mapper>
