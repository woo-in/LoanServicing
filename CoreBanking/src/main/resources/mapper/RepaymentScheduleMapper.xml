<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="hello.corebanking.domain.repayment.repository.RepaymentScheduleMapper">

    <select id="sumScheduledPrincipalByLoanAccountId" resultType="java.math.BigDecimal">
        SELECT COALESCE(SUM(scheduled_principal), 0)
        FROM   repayment_schedule
        WHERE  loan_account_id = #{loanAccountId}
          AND  repayment_schedule_status_id != #{excludedStatus}
    </select>

</mapper>
